# Template — do NOT commit real values.
# Populate via CI/CD (e.g. kubectl create secret or External Secrets Operator).
apiVersion: v1
kind: Secret
metadata:
  name: damagochat-secrets
  namespace: damagochat
type: Opaque
stringData:
  DATABASE_URL: "postgresql://user:CHANGE_ME@postgres.damagochat.svc.cluster.local:5432/damagochat"
  REDIS_URL: "redis://redis.damagochat.svc.cluster.local:6379"
  JWT_ACCESS_SECRET: "CHANGE_ME_64_CHARS_MIN"
  JWT_REFRESH_SECRET: "CHANGE_ME_64_CHARS_MIN_DIFFERENT"
  S3_ENDPOINT: "https://s3.amazonaws.com"
  S3_ACCESS_KEY: "CHANGE_ME"
  S3_SECRET_KEY: "CHANGE_ME"
  FCM_PROJECT_ID: "CHANGE_ME"
  FCM_CLIENT_EMAIL: "CHANGE_ME"
  FCM_PRIVATE_KEY: "CHANGE_ME"
  MEILISEARCH_KEY: "CHANGE_ME"
