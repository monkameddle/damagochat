apiVersion: v1
kind: Service
metadata:
  name: damagochat-api
  namespace: damagochat
  labels:
    app: damagochat-api
spec:
  type: ClusterIP
  selector:
    app: damagochat-api
  ports:
    - name: http
      port: 80
      targetPort: 3000
      protocol: TCP
